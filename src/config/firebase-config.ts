export const firebaseConfig = {
  projectId: "letmeask-a154a",
  appId: "1:your-app-id:web:your-app-id", // Você pode pegar isso no console do Firebase

  // Para usar apenas Cloud Functions, essas configurações básicas são suficientes
  // As outras chaves (apiKey, authDomain, etc.) são necessárias apenas para auth no frontend
};
